'use strict';

const a = require('..');

describe('A', () => {
    it('needs tests');
    it('needs tests');
    it('needs tests');
    it('needs tests');
    it('needs tests');
    it('needs tests');
    it('needs tests');
});
